[package]
name = "diskofflaner"
version = "1.0.3"
edition = "2021"

[dependencies]
colored = "2.1"
anyhow = "1.0"
eframe = "0.27"

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = [
    "fileapi",
    "handleapi",
    "winioctl",
    "winnt",
    "winerror",
    "processthreadsapi",
    "securitybaseapi",
    "ioapiset",
    "wincon"
] }
